// Génération des passes Google Wallet (JWT signé)
// Implémenté en Phase 3 avec Google Wallet API

async function generateGoogleWalletUrl({ client, marchand, serialNumber }) {
  // Phase 3 — Google Wallet API
  throw new Error('google-pass.js non encore implémenté — Phase 3');
}

module.exports = { generateGoogleWalletUrl };
