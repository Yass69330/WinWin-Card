// Génération des passes Apple Wallet (.pkpass)
// Implémenté en Phase 2 avec passkit-generator

async function generateApplePass({ client, marchand, serialNumber }) {
  // Phase 2 — passkit-generator
  throw new Error('apple-pass.js non encore implémenté — Phase 2');
}

module.exports = { generateApplePass };
